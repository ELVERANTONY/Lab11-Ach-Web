spring.application.name=PRJ_SPRING_BOOT_SECURITY_II

# Datasource MySQL (ajusta segun tu entorno)
spring.datasource.url=jdbc:mysql://localhost:3306/cursos_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=EAmysql12

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración para la inicialización de datos
# Solo se ejecutará data.sql si la tabla roles está vacía
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
spring.sql.init.continue-on-error=true

# Thymeleaf
spring.thymeleaf.cache=false
